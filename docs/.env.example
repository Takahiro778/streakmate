cat > .env.example <<'ENV'
# --- Application ---
APP_NAME=StreakMate
APP_HOST=localhost
APP_PORT=3000

# --- Rails ---
RAILS_ENV=development
TZ=Asia/Tokyo

# --- DB (dev/test はローカル想定) ---
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=streakmate_development
DB_TEST=streakmate_test

# --- Mailer (必要なら) ---
SMTP_ADDRESS=
SMTP_USER=
SMTP_PASSWORD=

# --- 3rd party keys (空でOK、ここに鍵は入れない) ---
# PAYJP_PUBLIC_KEY=
# PAYJP_SECRET_KEY=

# --- Production/Render は README 内リンク先手順で設定 ---
# DATABASE_URL=
# RAILS_MASTER_KEY=
ENV
