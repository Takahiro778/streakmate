<!-- カテゴリ切替タブ -->
<div class="flex items-center gap-2 mb-3">
  <%= link_to "リラックス", guide_path("relax"),
              class: "text-xs px-2 py-1 rounded border #{params[:id] == 'relax' ? 'bg-gray-100' : ''}" %>
  <%= link_to "就寝", guide_path("sleep"),
              class: "text-xs px-2 py-1 rounded border #{params[:id] == 'sleep' ? 'bg-gray-100' : ''}" %>
</div>

<!-- ランチャー本体（カテゴリはURLのidから） -->
<%= render "shared/suggestions_launcher",
           category: (params[:id].in?(%w[relax sleep]) ? params[:id] : "relax"),
           target_id: "suggestions_guides" %>
