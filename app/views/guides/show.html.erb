<div class="flex items-center gap-2">
  <%= button_to "ワンタップで提案",
                suggestions_path(category: params[:category] || "relax"),
                method: :post,
                form: { data: { turbo_stream: true } },
                class: "px-3 py-2 rounded-xl border shadow-sm bg-white text-sm hover:bg-gray-50" %>

  <div class="text-xs text-gray-500">
    ※API鍵不要・ローカルで動作（静的ルール）
  </div>
</div>

<div class="mt-3" id="suggestions">
  <!-- 初回空。押下で Turbo Stream により置換される -->
</div>
