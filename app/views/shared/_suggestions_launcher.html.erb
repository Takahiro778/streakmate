<!-- 共通: ワンタップ提案ランチャー -->
<div class="rounded-xl border bg-white shadow-sm p-3">
  <div class="flex items-center justify-between gap-3">
    <p class="text-sm text-gray-600">
      ひと息いれませんか？ 時間帯に合うミニ提案を3つ表示します。
    </p>

    <%= button_to "ワンタップで提案",
                  suggestions_path(category: (local_assigns[:category] || "relax")),
                  method: :post,
                  form: { data: { turbo_stream: true } },
                  class: "shrink-0 px-3 py-2 rounded-xl border bg-white text-sm hover:bg-gray-50" %>
  </div>

  <!-- Turbo Stream がここを置換します（ページごとに一意のIDを渡せます） -->
  <div class="mt-3" id="<%= local_assigns[:target_id] || 'suggestions' %>"></div>

  <p class="mt-1 text-[11px] text-gray-400">
    ※API鍵不要（静的ルール）/ 将来はLLMに差し替え可能
  </p>
</div>
