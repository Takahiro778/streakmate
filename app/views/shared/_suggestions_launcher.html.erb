<!-- 共通: ワンタップ提案ランチャー -->
<div class="card">
  <div class="flex items-center justify-between gap-3">
    <p class="text-sm text-gray-600">
      ひと息いれませんか？ 時間帯に合うミニ提案を3つ表示します。
    </p>

    <%= button_to "ワンタップで提案",
                  suggestions_path(
                    category: (local_assigns[:category] || "relax"),
                    target_id: (local_assigns[:target_id] || "suggestions")
                  ),
                  method: :post,
                  form: { data: { turbo_stream: true } },
                  class: "btn-secondary shrink-0" %>
  </div>

  <!-- Turbo Stream がここを置換します（ページごとに一意のIDを渡せます） -->
  <div class="mt-3" id="<%= local_assigns[:target_id] || 'suggestions' %>"></div>
</div>
