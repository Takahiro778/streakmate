<% if obj.errors.any? %>
  <div class="border border-red-300 bg-red-50 p-3 rounded mb-4">
    <p><strong><%= obj.errors.count %></strong> 件のエラーがあります。</p>
    <ul class="list-disc pl-5">
      <% obj.errors.full_messages.each do |m| %><li><%= m %></li><% end %>
    </ul>
  </div>
<% end %>
