<%= form_with model: [log, comment], data: { turbo_frame: "new_comment" }, class: "flex gap-2" do |f| %>
  <%= f.text_field :body, placeholder: "コメント（1〜200文字）", class: "input flex-1", maxlength: 200 %>
  <%= f.submit (comment.persisted? ? "更新" : "送信"), class: "btn-primary" %>
<% end %>
