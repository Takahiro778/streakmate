<h1 class="text-xl font-bold mb-3"><%= @user.nickname %></h1>

<div class="mb-3">
  <%= render "follows/button",  user: @user %>
</div>
<div class="mb-6">
  <%= render "follows/counts",  user: @user %>
</div>

<!-- 任意：そのユーザーの公開ログを数件 -->
<% @logs&.each do |log| %>
  <%= render "timeline/card", log: log %>
<% end %>
