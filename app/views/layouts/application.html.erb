<!DOCTYPE html>
<html>
  <head>
    <title>StreakMate</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="description" content="Build streaks with friends">

    <!-- OGP (site-wide) -->
    <meta property="og:site_name" content="StreakMate" />
    <meta property="og:title" content="StreakMate" />
    <meta property="og:description" content="Build streaks with friends" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= "#{request.base_url}/ogp.png" %>" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="StreakMate" />
    <meta name="twitter:description" content="Build streaks with friends" />
    <meta name="twitter:image" content="<%= "#{request.base_url}/ogp.png" %>" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <!-- ← body から pb-20 / safe-bottom を外す -->
  <body class="bg-gray-50 text-gray-900">
    <%= render "shared/header_nav" %>

    <!-- ヘッダー分の上余白 + フッター分の下余白を main 側で担保 -->
    <main class="pt-12 pb-footer md:pb-0">
      <div class="max-w-sm mx-auto px-4 pt-3">
        <%= render "shared/flash" %>
        <%= yield %>
      </div>
    </main>

    <%= render "shared/footer_nav" %>
  </body>
</html>
