#!/usr/bin/env bash
set -euo pipefail

# foreman が無ければ入れる（初回のみ）
if ! command -v foreman >/dev/null 2>&1; then
  echo "Installing foreman..."
  gem install foreman
fi

exec foreman start -f Procfile.dev "$@"
